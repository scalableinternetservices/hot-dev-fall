<%= form_with(model: contract, local: true) do |form| %>
  <% if contract.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contract.errors.count, "error") %> prohibited this contract from being saved:</h2>

      <ul>
      <% contract.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sharer %>
    <%= form.number_field :sharer, id: :contract_sharer %>
  </div>

  <div class="field">
    <%= form.label :joiners %>
    <%= form.number_field :joiners, id: :contract_joiners %>
  </div>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, id: :contract_username %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password, id: :contract_password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
